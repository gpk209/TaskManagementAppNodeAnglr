# Environment Configuration

# Server Configuration
PORT=5000
NODE_ENV=development

# Database Configuration
MONGODB_URI=mongodb://localhost:27017/taskmanagement

# JWT Configuration - Access Token (short-lived, stored in memory on client)
JWT_SECRET=your-super-secret-jwt-key-change-in-production-min-32-chars
JWT_EXPIRES_IN=15m

# JWT Configuration - Refresh Token (long-lived, stored in HTTP-only cookie)
JWT_REFRESH_SECRET=your-refresh-token-secret-change-in-production-min-32-chars
JWT_REFRESH_EXPIRES_IN=7d

# Cookie Configuration
COOKIE_SAME_SITE=lax

# Logging Configuration
LOG_LEVEL=debug

# CORS Configuration
CORS_ORIGIN=http://localhost:4200
